name: schedule

permissions:
  contents: write

on:
  workflow_dispatch:
  schedule:
    - cron: '00 12 * * 6'

jobs:
  call-update:
    uses: js6pak/libil2cpp-archive/.github/workflows/update.yml@master
    secrets: inherit
